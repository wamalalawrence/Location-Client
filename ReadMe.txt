 Use mvn package to build the project

 To run the application use the following command from within the Location-Service folder:
  java -jar target/Location-CLient-1.0-SNAPSHOT.one-jar.jar

This client executes a GET request to the Location-Service api and creates a
CSV file (data.csv) in a cross-platform way to user's home directory